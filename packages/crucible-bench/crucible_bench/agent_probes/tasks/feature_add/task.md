This sandbox has a small calculator CLI in main.py, a few operation functions
in ops.py, and tests in test_ops.py. Please:

1) Add a `power(a, b)` function to ops.py that returns a ** b.
2) Add a CLI flag `--pow` to main.py that calls it.
3) Add a test for `power` to test_ops.py covering at least two cases.
4) Verify all tests still pass by running test_ops.py.

Match the style of the existing code.

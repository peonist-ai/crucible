There is a `merge.py` in the sandbox with a function that's supposed to deep-
merge configuration dicts, plus `test_merge.py` with tests. Run the tests, find
the bug(s), and fix `merge.py` so all tests pass. The function's docstring is
the spec — be careful that fixing one test doesn't break another.
